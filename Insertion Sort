#include <iostream>
using namespace std;

int main() 
{
	int size;int key;int i;int j;int arr[i];
	cin >> size;
for(i=1;i<size;i++)
{
	key=arr[i];
	j=i-1;
	while(j>=0 && arr[j]>key)
	{
		arr[j+1]=arr[j];
		j=j-1;
	}
	arr[j+1]=key;
}
for (i = 0; i <size; i++)  
        cout << arr[i] << " ";
	return 0;
}
